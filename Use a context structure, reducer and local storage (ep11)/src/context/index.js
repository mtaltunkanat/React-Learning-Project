export { default as AuthProvider, useAuth } from "./AuthContext" ;
export { default as SiteProvider, useSite } from "./SiteContext" ;

/* export { yada yukarıdakileri export ve default as SiteProvider yazıcak isimlendirilmemiş etiket olmicak
      AuthContext,
      SiteContext,
      useAuth,
      useSite
} */